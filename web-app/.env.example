# URL of the deployed Kathakaar FastAPI backend (no trailing slash).
# Vite inlines this at BUILD time, so set it in your host's build settings.
# Local dev auto-uses http://localhost:8000 when this is unset.
VITE_API_BASE=https://your-backend.onrender.com
